import React from "react";

function Contact() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        width: "100%",
        height: "100vh",
      }}
    >
      Contact
    </div>
  );
}

export default Contact;
